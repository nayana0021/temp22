*.class
.DS_Store

index.txt

#확장자가 .a 인 파일 무시
*.a

#윗 라인에 확장자가 a인 파일은 무시했지만 lib.a 는 무시하지 않음
!lib.a

#현재 디렉토리에 있는 TODO 파일은 무시하고 subdir/TODO 처럼 하위 디렉토리에 있는 파일은
#무시하지 않음 - 하위 디렉토리 파일은 무시하라고
/TODO

#build 디렉토리에 있는 모든 파일은 무시
build/

#doc 폴더 아래에 모든 txt 파일 무시(doc/server/test.txt 파일은 무시하지 않음)
#바로 밑에 파일만 무시
doc/*.txt

#doc 폴더 아래의 모든 .pdf 파일 무시(doc/server/1.pdf무시)
doc/**/*.pdf
